---
sidebar_position: 3
---

# Advanced

import AppUrl from '@site/src/components/AppUrl';
import DocCardList from '@theme/DocCardList';
import {useCurrentSidebarCategory} from '@docusaurus/theme-common';

This section explains Enscribe's contract and platform architecture in more detail.

We cover ENS in additional depth and support for ENS in L1 and L2 chains.

<DocCardList
  items={useCurrentSidebarCategory().items.filter(
    (item) => item.label?.toLowerCase() !== 'overview',
  )}
/>
